-- get_global_cache-- set_global_cache--local function  install(service_name)--     local processes = processes()--     for i,v in ipairs(processes) do--      local res = string.find(v['cmdline'], service_name)--      if  res ~= nil  then--      print(service_name)--      print(11)--       return true--      end--     end--     return false--endinstall_mysql = falseinstall_docker = falselocal function check()    local processes = processes()    for i,v in ipairs(processes) do        if  string.find(v['cmdline'], "mysqld") ~= nil then            install_mysql = true        end        if  string.find(v['cmdline'], "docker") ~= nil then            install_docker = true        end    end    --print(install_mysql)    set_global_cache('install_mysql', install_mysql)    set_global_cache('install_docker', install_docker)endcheck()--print(get_global_cache('install_mysql'))